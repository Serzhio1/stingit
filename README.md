<img width="1024" height="1024" alt="image_2025-11-28_23-47-12" src="https://github.com/user-attachments/assets/9f26ff51-0bc8-4682-95fc-47cc20599f7a" />

# Stingit  
Stingit - это набор кастомных git hooks, которые будут вас больно жалить, чтобы добиться повышения качества разработки


## Какую проблему решает Stingit

Git предоставляет механизм запуска пользовательских скриптов на разных этапах разработки в случае возникновения определённых событий — [Git hooks](https://git-scm.com/book/ru/v2/Настройка-Git-Хуки-в-Git), но большинство команд либо не используют их, либо используют минимально. В результате:

- в репозиторий попадают некорректные коммит-сообщения  
- код пушится без форматирования или без прохождения тестов  
- важные правила команды соблюдаются вручную  
- процесс разработки теряет консистентность и скорость  
- многие другие проблемы на разных этапах при работе с git

Stingit - это набор кастомных гит-хуков, которые ты можешь использовать в своем проекте, чтобы решить одну из вышеперечисленных проблем

## Примеры Git Hooks

| Хук | Когда срабатывает | Типичные задачи |
|-----|-------------------|----------------|
| pre-commit | перед созданием коммита | форматирование, линтинг, тесты |
| commit-msg | после ввода сообщения коммита | валидация формата сообщения коммита |
| pre-push | перед пушем | запуск тестов, запрет на пуш в main |
| pre-merge-commit | перед созданием merge-коммита | проверки конфликтов и правил команды |

## Структура проекта

Каждый хук c описанием и тестами находистя в отдельной директории:

```
stingit/
│
├── README.md 
│
├── commit-msg-hook/
│   ├── commit-msg-README.md
│   ├── commit-msg
│   └── commit-msg-tests/
│
├── pre-commit-hook/
│   ├── pre-commit-README.md
│   ├── pre-commit
│   └── pre-commit-tests/
│
├── pre-merge-commit-hook/
│   ├── pre-merge-commit-README.md
│   ├── pre-merge-commit
│   └── pre-merge-commit-tests/
│
└── ... другие хуки ...
```

## Как добавить хук, работая на unix-подобной системе

1. Из данного проекта просто скопируй все содержимое нужного тебе хука
2. В рамках своего проекта перейди в директорию ```/.git/hooks/```. Например, это может выглядеть так:
```bash
cd your-project/.git/hooks/
```
3. Внутри директории «hooks» откройте для редактирования тот хук, который хочешь добавить. Например, с помощью радактора ```nano``` это делается так:
```bash
ls 
nano commit-msg.sample
```
4. Убери из данного файла код и комментарии, которые были в нем для примера, а после этого вставь скопированный хук
5. С помощью сочетания клавиш ```Ctrl+x``` сохрани изменения и, перед выходом, измени название файла, убрав из него часть ```.sample```
6. На последнем шаге остается сделать данный файл исполняемым. Для этого выполни такую команду:
```bash
chmod +x commit-msg
```
7. Отлично, вот ты и добавил свой первый хук!

## Как добавить хук, работая на Windows🥲

Ничего сложного, просто приготовься использовать ```Git Bash``` или ```WSL```, и выполяй действия, описанные в разделе ```Как добавить хук, работая на unix-подобной системе```

## Обратная связь

Следи за обновлениями и сообщай обратную связь по Stingit в [Telegram-канале](https://t.me/stingit)
